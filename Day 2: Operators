void solve(double meal_cost, int tip_percent, int tax_percent) 
{
double tip,tax,tp,tx;

tp=tip_percent;
tx=tax_percent;

tip=meal_cost*(tp/100);
tax=meal_cost*tx/100;
int total=(int)round(meal_cost+tip+tax);

printf("%d",total);
}
